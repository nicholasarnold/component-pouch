import React from 'react';
import { Logo } from '../Logo/Logo';

export const Header = () => {
  return (
    <header>
      <div className="header container">
        <Logo />
      </div>
    </header>
  )
}